packages:
  - app
  - storybook

# pnpm 10 blocks dependency build scripts by default. These ship native binaries
# / postinstall steps that the toolchain needs, so allow them explicitly.
onlyBuiltDependencies:
  - esbuild
  - "@tailwindcss/oxide"
